# `@asmadsen/eslint-config`

> My personal [ESLint](https://eslint.org) config.

## Usage

**Install**:

```bash
$ yarn add -D @asmadsen/eslint-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "eslintConfig": {
    "extends": [
      "@asmadsen"
    ]
  },
}
```
